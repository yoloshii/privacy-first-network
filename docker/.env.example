# =============================================================================
# Privacy Router - Docker Environment Configuration
# =============================================================================
# Copy this file to .env and customize for your setup
#
# Required values are marked with (REQUIRED)
# =============================================================================

# -----------------------------------------------------------------------------
# VPN Configuration (REQUIRED - from your VPN provider)
# -----------------------------------------------------------------------------

# Your VPN internal IP address (from provider config)
# Example for Mullvad: 10.64.123.45/32
# Find this in your WireGuard config as "Address"
VPN_IP=10.64.0.1/32

# VPN server endpoint IP address (NOT hostname!)
# Use: dig +short vpn.mullvad.net (or your provider's hostname)
# IMPORTANT: Must be an IP, not a hostname
VPN_ENDPOINT_IP=185.213.154.68

# VPN server port (usually 51820)
VPN_ENDPOINT_PORT=51820

# -----------------------------------------------------------------------------
# LAN Configuration (adjust to match your network)
# -----------------------------------------------------------------------------

# Docker host's LAN-facing network interface
# Find with: ip link show
LAN_INTERFACE=eth0

# Your LAN subnet
LAN_SUBNET=192.168.1.0/24

# Your modem/upstream router IP (existing gateway)
LAN_GATEWAY=192.168.1.1

# IP address for the privacy-router container on your LAN
# This is what LAN devices will use as their gateway
# Choose an unused IP in your subnet
CONTAINER_LAN_IP=192.168.1.5

# -----------------------------------------------------------------------------
# Watchdog Configuration (auto-recovery)
# -----------------------------------------------------------------------------

# Enable automatic tunnel recovery
WATCHDOG_ENABLED=true

# Seconds between health checks
WATCHDOG_INTERVAL=30

# Number of failures before restart
WATCHDOG_FAIL_THRESHOLD=3

# IPs to ping for connectivity test (space-separated)
PROBE_TARGETS=1.1.1.1 8.8.8.8 9.9.9.9

# -----------------------------------------------------------------------------
# General
# -----------------------------------------------------------------------------

# Timezone for logs
TZ=UTC
