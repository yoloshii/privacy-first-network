# =============================================================================
# Privacy Router Stack - Mullvad Configuration
# =============================================================================
# This stack was developed and tested with Mullvad VPN
# Copy to .env and fill in your Mullvad credentials
#
# Get your WireGuard config: https://mullvad.net/en/account/wireguard-config
# =============================================================================

# Timezone
TIMEZONE=UTC

# =============================================================================
# MULLVAD WIREGUARD CONFIGURATION
# =============================================================================
# From your downloaded Mullvad WireGuard config file

# Your chosen Mullvad server endpoint
# Find servers: https://mullvad.net/en/servers
# Format: IP:PORT (use IP not hostname)
# Common ports: 51820 (default), 53 (DNS), 443 (HTTPS)
VPN_ENDPOINT=MULLVAD_SERVER_IP:51820

# Your Mullvad internal address (from config, usually 10.x.x.x/32)
VPN_ADDRESS=10.x.x.x/32

# Your Mullvad private key (from config file [Interface] section)
VPN_PRIVATE_KEY=YOUR_MULLVAD_PRIVATE_KEY

# Mullvad server public key (from config file [Peer] section)
VPN_SERVER_PUBLIC_KEY=MULLVAD_SERVER_PUBLIC_KEY

# Route all traffic through VPN
VPN_ALLOWED_IPS=0.0.0.0/0

# =============================================================================
# AMNEZIAWG OBFUSCATION
# =============================================================================
# For DPI bypass in restrictive networks
# Mullvad uses standard WireGuard - these values add obfuscation layer

AWG_JC=4
AWG_JMIN=40
AWG_JMAX=70
AWG_S1=0
AWG_S2=0
AWG_H1=1
AWG_H2=2
AWG_H3=3
AWG_H4=4

# =============================================================================
# DNS CONFIGURATION
# =============================================================================
# Mullvad DNS-over-HTTPS servers
# All queries encrypted and go through VPN tunnel

# With ad/tracker blocking (recommended)
UPSTREAM_DNS=https://adblock.dns.mullvad.net/dns-query

# Without blocking (if you prefer AdGuard filters only)
# UPSTREAM_DNS=https://dns.mullvad.net/dns-query

# Extended blocking (ads + trackers + adult)
# UPSTREAM_DNS=https://all.dns.mullvad.net/dns-query
