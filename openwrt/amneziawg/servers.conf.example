# =============================================================================
# AmneziaWG Server List for Failover
# =============================================================================
#
# Format: NAME ENDPOINT_IP PORT PUBLIC_KEY
#
# - NAME: Friendly name for logging
# - ENDPOINT_IP: Server IP address (not hostname)
# - PORT: WireGuard port (usually 51820)
# - PUBLIC_KEY: Server's public key (use "-" if same as base config)
#
# First server is PRIMARY - watchdog will failback to it when available
# Order matters - servers are tried in sequence during failover
#
# Get server list from your VPN provider:
# - Mullvad: https://mullvad.net/en/servers
# - IVPN: https://www.ivpn.net/status
#
# =============================================================================
# IMPORTANT: PUBLIC KEY GOTCHA
# =============================================================================
#
# Different cities/regions typically have DIFFERENT public keys!
# Using "-" only works for servers that share the SAME key as your base config.
#
# SAFE: All servers from same city cluster (e.g., all us-lax-wg-xxx)
# UNSAFE: Mixing cities (us-lax + us-sjc) with "-" - different keys will fail!
#
# If mixing cities, get each city's public key from your provider and specify it.
#
# =============================================================================

# Primary server (preferred)
us-lax-wg-001   CHANGE_ME_IP   51820   CHANGE_ME_PUBKEY

# Failover servers (SAME CITY = same public key, can use "-")
us-lax-wg-002   CHANGE_ME_IP   51820   -
us-lax-wg-003   CHANGE_ME_IP   51820   -
us-lax-wg-004   CHANGE_ME_IP   51820   -
us-lax-wg-005   CHANGE_ME_IP   51820   -

# =============================================================================
# Example with explicit keys (for mixing cities):
# =============================================================================
#
# # Same city cluster - can use "-" after first key
# us-lax-wg-001   203.0.113.10    51820   LAX_PUBLIC_KEY_HERE
# us-lax-wg-002   203.0.113.11    51820   -
# us-lax-wg-003   203.0.113.12    51820   -
#
# # Different city - MUST specify its own key
# us-sjc-wg-001   198.51.100.10   51820   SJC_PUBLIC_KEY_HERE
# us-sea-wg-001   198.51.100.20   51820   SEA_PUBLIC_KEY_HERE
#
# IPs above are RFC 5737 documentation ranges - replace with real server IPs
# =============================================================================
